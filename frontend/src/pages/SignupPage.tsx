import SignupForm from "@/components/user/member/signup/SignupForm";

/**
 * SignupPage
 * - 페이지는 조립만 담당
 * - 실제 회원가입 UI/로직은 SignupForm 컴포넌트에서 처리
 */
export default function SignupPage() {
  return <SignupForm />;
}